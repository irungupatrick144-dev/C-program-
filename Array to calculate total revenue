
  /*
    Name: Patrick Kuria
    Registration Number: CY101/G/26479/25

    Descriptions:
     program  to calculates two things:
    1. The total revenue for the whole week.
    2. The average revenue earned per day.
*/

#include <stdio.h>

int main() {
    float revenue[7], total = 0, average;
    int i;

    // Input revenue for each day
    for(i = 0; i < 7; i++) {
        printf("Enter revenue for day %d: ", i + 1);
        scanf("%f", &revenue[i]);
        total = total + revenue[i];
    }

    average = total / 7;

    // Output results
    printf("\nTotal weekly revenue = %.2f\n", total);
    printf("Average daily revenue = %.2f\n", average);

    return 0;
}
program calculates two things:
    1. The total revenue for the whole week.
    2. The average revenue earned per day.
*/

#include <stdio.h>

int main() {
    float revenue[7], total = 0, average;
    int i;

    // Input revenue for each day
    for(i = 0; i < 7; i++) {
        printf("Enter revenue for day %d: ", i + 1);
        scanf("%f", &revenue[i]);
        total = total + revenue[i];
    }

    average = total / 7;

    // Output results
    printf("\nTotal weekly revenue = %.2f\n", total);
    printf("Average daily revenue = %.2f\n", average);

    return 0;
}
